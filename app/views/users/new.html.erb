<div class="users-new-wrapper">
<div class="container">
  <div class="row">
    <div class="col-md-offset-4 col-md-4 users-new-container">
      <h1 class="text-center text-white">Sign in</h1>
      <%= form_for @user do |f| %>
      <!--form_forはformを作るためのメソッド  -->
      <!--@user の箇所にモデル名が入る。controllerと紐づいている。-->
      <div class="form-group">
        <%= f.label :name, class: 'text-white' %>
        <%= f.text_field :name, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :email, class: 'text-white' %>
        <%= f.text_field :email, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :password, class: 'text-white' %>
        <%= f.password_field :password, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :password_confirmation, class: 'text-white' %>
        <%= f.password_field :password_confirmation, class: 'form-control' %>
      </div>


      <%= f.submit"登録", class:'btn-block btn-white' %>
      <% end %>
      <%= link_to 'ログインはこちら', '#', class: 'text-white' %>
    </div>
  </div>
</div>
</div>

<!--form_forメソッドのなかでf.submitメソッドを使うことで、Railsが自動的に、適したアクションに値を送信する。今回の場合 create actionに登録データは送信される -->
